{
  "entities": {
    "TeamMember": {
      "title": "Team Member",
      "description": "A coach or user with access to the team data.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "The user's UID." },
        "joinedAt": { "type": "string", "format": "date-time" },
        "role": { "type": "string", "enum": ["coach", "player"] },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "lastStatsAccess": { "type": "string", "format": "date-time" },
        "statsAccessCount": { "type": "number" },
        "lastActive": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "joinedAt", "role"]
    },
    "PlayerAccessLog": {
      "title": "Player Access Log",
      "description": "Record of a player Google account accessing team stats.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "role": { "type": "string" },
        "accessedAt": { "type": "string", "format": "date-time" },
        "view": { "type": "string" },
        "device": { "type": "string" }
      },
      "required": ["id", "uid", "email", "role"]
    },
    "Setting": {
      "title": "Setting",
      "description": "Team settings, core config.",
      "type": "object",
      "properties": {
        "roster": { "type": "array" },
        "savedRosters": { "type": "object" },
        "savedLineups": { "type": "object" },
        "teamName": { "type": "string" },
        "coachCode": { "type": "string" },
        "playerCode": { "type": "string" },
        "playerAccessEnabled": { "type": "boolean", "description": "Controls whether players can access team stats and data." }
      }
    },
    "Opponent": {
      "title": "Opponent",
      "description": "Scouting and opponent details.",
      "type": "object",
      "properties": {
        "defaultLineup": { "type": "array" },
        "notes": { "type": "object" },
        "setterId": { "type": "string" },
        "liberoId": { "type": "string" }
      }
    },
    "Match": {
      "title": "Match",
      "description": "A scheduled match.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "teamId": { "type": "string" },
        "date": { "type": "string", "format": "date-time" },
        "type": { "type": "string" },
        "title": { "type": "string" },
        "opponent": { "type": "string" },
        "format": { "type": "string" },
        "cap": { "type": "string" }
      },
      "required": ["id", "teamId", "date", "type", "opponent", "format"]
    },
    "MatchSet": {
      "title": "Set",
      "description": "A distinct set in a match.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "teamId": { "type": "string" },
        "matchId": { "type": "string" },
        "setNum": { "type": "number" },
        "scoreUcc": { "type": "number" },
        "scoreOpp": { "type": "number" }
      },
      "required": ["id", "teamId", "matchId", "setNum", "scoreUcc", "scoreOpp"]
    },
    "Stat": {
      "title": "Stat",
      "description": "An action or event that occurred in a match.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "teamId": { "type": "string" },
        "matchId": { "type": "string" },
        "setId": { "type": "string" },
        "playerId": { "type": "string" },
        "category": { "type": "string" },
        "metric": { "type": "string" },
        "value": { "type": "number" },
        "isOpponent": { "type": "boolean" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "teamId", "matchId", "setId", "playerId", "category", "metric", "timestamp"]
    }
  },
  "firestore": {
    "/teams/{teamId}/members/{userId}": {
      "schema": { "$ref": "#/entities/TeamMember" },
      "description": "Users who can access this team's data."
    },
    "/teams/{teamId}/player_access_logs/{logId}": {
      "schema": { "$ref": "#/entities/PlayerAccessLog" },
      "description": "Logs of players accessing team statistics."
    },
    "/teams/{teamId}/settings/{settingId}": {
      "schema": { "$ref": "#/entities/Setting" },
      "description": "The team settings."
    },
    "/teams/{teamId}/opponents/{opponentId}": {
      "schema": { "$ref": "#/entities/Opponent" },
      "description": "Recorded opponents for the team."
    },
    "/teams/{teamId}/matches/{matchId}": {
      "schema": { "$ref": "#/entities/Match" },
      "description": "The matches scheduled or played by the team."
    },
    "/teams/{teamId}/sets/{setId}": {
      "schema": { "$ref": "#/entities/MatchSet" },
      "description": "The sets played in matches."
    },
    "/teams/{teamId}/stats/{statId}": {
      "schema": { "$ref": "#/entities/Stat" },
      "description": "The atomic actions that happened during sets."
    }
  }
}
